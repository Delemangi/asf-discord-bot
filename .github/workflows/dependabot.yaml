name: Dependabot Auto-Merge

on:
  pull_request:
    types: [opened, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    uses: Delemangi/.github/.github/workflows/dependabot.yaml@main
    secrets: inherit
